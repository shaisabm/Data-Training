{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
      "builder": "NIXPACKS"
  },
  "deploy": {
      "startCommand": "python -m venv .venv && source .venv/bin/activate && python -m pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py make makemigrations && python manage.py migrate && gunicorn --workers=4 DataTraining.wsgi ",
      "restartPolicyType": "ON_FAILURE",
      "restartPolicyMaxRetries": 5
  }
}
