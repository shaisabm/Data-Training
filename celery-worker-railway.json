{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": ". /opt/venv/bin/activate && python manage.py collectstatic --noinput && python manage.py migrate && celery -A DataTraining worker --uid=nobody --gid=nogroup",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}